let course = ["Android","java","php","angular"];

console.log(
	course.map(course => {
		return course.toUpperCase();
	})
);

// let scores = [9, 2, 8, 4, 7, 3, 1, 8];  //sai chua biet sua

// scores.sort(
// 	(x, y) => x < y ? true : false //x > y giam dan
// );

// console.log(scores);